# Required: 32-byte hex key for encrypting API keys at rest
# Generate with: openssl rand -hex 32
ENCRYPTION_SECRET=

# Optional: Custom database path (default: ./data/rfi-generator.db)
# DATABASE_URL=

# Optional: Auth secret for session management
# Generate with: openssl rand -hex 32
AUTH_SECRET=
